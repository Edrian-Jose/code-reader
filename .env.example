# OpenAI API Key (required for embeddings)
OPENAI_API_KEY=test
# OpenAI Base URL (optional, for Azure OpenAI, proxies, or custom endpoints)
# Examples:
#   Azure OpenAI: https://your-resource.openai.azure.com
#   Proxy: https://your-proxy.com/v1
#   Custom: https://api.your-service.com/v1
# OPENAI_BASE_URL=

# MongoDB Connection URIs - Dual connection with automatic fallback
# Primary: Atlas Local (Docker deployment) - better performance with vector search
MONGODB_ATLAS_URI=mongodb://localhost:58746/?directConnection=true

# Fallback: Standard MongoDB - always-on local database
MONGODB_LOCAL_URI=mongodb://localhost:27017

# Legacy support: MONGODB_URI (if set, overrides both Atlas and Local)
# MONGODB_URI=mongodb://localhost:27017

# Server Port (optional, defaults to 3100)
CODE_READER_PORT=3100

# Log Level (optional, defaults to info)
LOG_LEVEL=info

# ============================================================================
# Confluence Integration (Optional - for documentation enrichment)
# ============================================================================
# Atlassian Confluence Instance URL (e.g., https://your-domain.atlassian.net)
# ATLASSIAN_INSTANCE_URL=

# Atlassian Username (email address used for Confluence login)
# ATLASSIAN_USERNAME=

# Atlassian API Token (create at: https://id.atlassian.com/manage-profile/security/api-tokens)
# ATLASSIAN_API_TOKEN=

# Note: If these are not set, Confluence integration will be disabled
# The documentation generator will still work using only code analysis + CLAUDE.md
